name: Keep Gateway Alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl API Gateway
        run: curl -s https://lumo-api-gateway.onrender.com/heartbeat > /dev/null
